export interface SensorDetail {
  id: number;
  name: string;
  cloud: string;
  connection: string;
  format: string;
  frequency: number;
  minRange: number;
  maxRange: number;
  timeInterval: number;
}
